<div class="card mb-4">
    <div class="card-header">
        <h5 class="card-title mb-0">
            <i class="fas fa-cube me-2"></i>
            Dummy Component
        </h5>
    </div>
    <div class="card-body">
        <p class="card-text">This is a simple dummy component that demonstrates a template-only component without a PHP class.</p>
        <p class="text-muted">
            <small>
                <i class="fas fa-info-circle me-1"></i>
                This component uses only a Twig template and doesn't require any server-side logic.
            </small>
        </p>
        <div class="alert alert-info" role="alert">
            <strong>Template-only components</strong> are perfect for static content, simple UI elements, or components that only need client-side interactivity.
        </div>
    </div>
</div>